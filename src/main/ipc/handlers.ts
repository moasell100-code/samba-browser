import type { BrowserWindow } from 'electron'
import type { TabManager } from '../browser/tab-manager'

// IPC 채널 등록. Task 7 에서 구현
export function registerIpc(_win: BrowserWindow, _tabs: TabManager): void {
  // Task 7 에서 구현
}

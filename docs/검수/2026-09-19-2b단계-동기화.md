# 2b단계 동기화 실검수 (2026-09-19)

환경: Supabase 프로젝트 `samba-browser`(서울), PC A = 개발 앱(기존 데이터 있음), PC B = 같은 PC 의 빈 데이터 폴더(`SAMBA_USER_DATA`)로 띄운 빌드 산출물. 값(비밀번호·복구 키·토큰)은 기록하지 않음.

| # | 완료 기준 | 결과 | 비고 |
|---|---|---|---|
| 1 | PC A 가입 → 키마스터 설정 → 계정·북마크 저장 | 성공 | 가입 시 이메일 확인이 켜져 있어 로그인 실패 → 대시보드에서 계정 확인 후 성공. 이후 "Confirm email" 끔 |
| 2 | PC B 같은 계정 로그인 → 같은 마스터 비밀번호 → PC A 데이터 표시·복호화 | 부분 | 키 재료 동기화로 B 키마스터 "설정됨(잠김)" 진입 OK. 로그인 전 데이터 미전송 → backfill 구현 후 A 2,100건 푸시 완료. B 는 계정 607/북마크 0 에서 정지 → 원인 2건(backfill 옛 updated_at, 풀 단일 페이지) 수정 중 |
| 3 | 오프라인 수정 → 복귀 시 전송 | 미검증 | |
| 4 | 양쪽 동시 수정 LWW | 미검증 | |
| 5 | 북마크 합집합 | 성공 | A→B, B→A 생성 전파 확인 |
| 5-1 | 북마크 삭제 전파(tombstone) | 성공(수정 후) | 삭제 표식의 `updated_at` 이 옛 값이라 커서에 걸러지던 버그 발견·수정(8654a60) |
| 6 | A 에서 B 원격 로그아웃 → B 로그아웃+잠금 | 성공 | 즉시 감지, 자동 로그아웃·잠금. 단 재시작 시 저장 세션으로 되살아나는 결함 발견 → 세션 삭제·재등록 금지로 수정(bf3e245) |
| 7 | Supabase 에 `audit` 표 없음 | 성공 | public 표 8개(accounts_sync, bookmarks_sync, devices, profiles, recipes, settings_sync, vault_items_sync, workspaces), 전부 RLS |
| 8 | Claude 구독 감지 → 작업별 모델 | 부분 | 설정 화면에서 "연결됨" 표시 확인. 지정 모델로 실행되는 로그 확인은 미실시 |
| 9 | 작업공간 분리 | 성공 | 새 작업공간 전환 시 북마크 0·계정 0, 복귀 시 434·1,550 복원, 삭제까지 API 로 확인 |
| 10 | 내보내기 CSV 되읽기 | 미검증 | |
| + | 설정 동기화(searchEngine) A→B | 성공 | |
| + | 기기 목록 | 성공 | 양쪽에서 2대 표시, "이 기기" 구분 |
| + | 채팅 기록 동기화 | 구현 | 로컬 표 `chats`/`chat_messages` + 원격 `chats_sync`/`chat_messages_sync`(schema.sql 재실행 완료). 실검증은 재기동 후 |
| + | 확장 설치(웹스토어·다른 브라우저 가져오기) | 구현 | 실검증 미실시 |
| + | 캡차/2FA 넘김 카드·자동 재개, 로그인 세션 유지 | 구현 | 실검증 미실시 |
